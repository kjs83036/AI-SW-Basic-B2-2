# 🚀 B2-2 실습 가이드 플레이북 (3인 팀 완벽 실행 대본 + GitLens 시각화 가이드)

이 문서는 3명의 팀원(또는 1인 시뮬레이션)이 B2-2 미션의 모든 기능 요구사항과 평가 기준을 100% 충족하며 실습을 완료할 수 있도록 작성된 **단계별 상세 실행 매뉴얼**입니다.

> 💡 **학습 팁:** 모든 작업은 **기본 CLI 터미널 명령어**를 직접 실행하여 동작 원리를 먼저 이해하고, 각 단계마다 제공되는 **`🔍 GitLens 시각적 확인`** 팁을 통해 Git의 내부 상태(포인터, 트리, 그래프, Diff)가 어떻게 변하는지 눈으로 확인하며 학습합니다.

---

## 📋 실습 개요 및 역할 분담표

| 구분 | 팀원 A (Kim) | 팀원 B (Lee) | 팀원 C (Park) |
| :--- | :--- | :--- | :--- |
| **주요 역할** | 팀 리드 & 수학 유틸 | 문자열 유틸 & 충돌 해결 | 날짜 유틸 & 테스트 통합 |
| **담당 모듈** | `src/utils/math_ops.py` | `src/utils/string_ops.py` | `src/utils/date_ops.py` |
| **담당 PR** | PR #1, PR #4 (리팩토링) | PR #2 (충돌1), PR #5 (충돌2) | PR #3, PR #6 (종합 테스트) |
| **트러블슈팅** | `commit --amend`, `git stash` | `reset --soft HEAD~1` | `git revert` |

---

## 🛠️ Step 0: 저장소 초기화 및 브랜치 보호 규칙 설정

### 1. 팀 리드(팀원 A) - GitHub 저장소 생성 및 초기 파일 커밋
```bash
# 1. 로컬 저장소 생성
mkdir team-python-utils
cd team-python-utils
git init -b main

# 2. 기본 디렉토리 및 협업 문서 준비
mkdir -p docs src/utils tests .github/ISSUE_TEMPLATE

# (문서 및 템플릿 파일 복사 후 첫 커밋)
git add .
git commit -m "chore: initial commit with basic project structure and contributing guides"

# 3. GitHub 원격 저장소 연결 및 푸시
git remote add origin https://github.com/<your-org-or-user>/team-python-utils.git
git branch -M main
git push -u origin main
```

### 2. GitHub 저장소 설정 (Branch Protection Rule)
- GitHub 저장소 웹 페이지 이동 -> **Settings** -> **Branches** -> **Add branch ruleset / protection rule**
  - **Branch name pattern:** `main`
  - ✅ **Require a pull request before merging** (체크)
  - ✅ **Require approvals:** `1` (최소 1명 승인)
  - ✅ **Do not allow bypassing the above settings**
  - Save changes

> 🔍 **GitLens 시각적 확인 (Commit Graph):**
> - VS Code 좌측 액티비티 바에서 **GitLens 아이콘** 클릭 → **Commit Graph** 패널 열기
> - `main` 브랜치와 `origin/main`이 첫 번째 커밋(`chore: initial commit...`)을 동일하게 가리키고 있는지 확인합니다.

---

## 🏃 Sprint 1: 기본 기능 구현 & 순환 코드 리뷰 & 충돌 1 실습

### 🔹 [Round 1-1] 팀원 A: 수학 유틸리티 개발 (PR #1)

1. **GitHub Issue 생성:**
   - 제목: `[FEAT] 기본 수학 연산 및 통계 유틸리티 구현` (이슈 `#1`)
2. **브랜치 생성 및 작업:**
   ```bash
   git checkout -b feature/kim-math-utils
   # src/utils/math_ops.py 작성 및 tests/test_math_ops.py 작성
   git add src/utils/math_ops.py tests/test_math_ops.py
   git commit -m "feat: add basic math utility functions"
   ```
3. **트러블슈팅 실습 1 (`commit --amend`):**
   ```bash
   # 누락된 타입 힌트와 docstring 보강 후 커밋 수정
   git add src/utils/math_ops.py
   git commit --amend -m "feat: add power function with type annotations and docstring"
   git push origin feature/kim-math-utils
   ```
   > 🔍 **GitLens 시각적 확인 (Amend 커밋 검증):**
   > - **Commit Graph**에서 방금 작성한 커밋이 2개로 분기되지 않고, 기존 커밋 해시가 갱신되며 **단 1개의 깔끔한 커밋**으로 유지되는 것을 확인합니다.

4. **PR #1 생성 (GitHub UI):**
   - 제목: `feat: add math utility functions and exports`
   - 본문:
     ```markdown
     Closes #1
     ## 변경 사항 (What)
     - 기본 사칙연산(`add`, `subtract`, `multiply`, `divide`) 및 `power`, `calculate_average` 구현
     ## 변경 이유 (Why)
     - 공통 수치 계산 모듈 제공
     ## 테스트 (How)
     - pytest tests/test_math_ops.py 통과
     ```
5. **팀원 B의 코드 리뷰 (Line Comment):**
   - 파일: `src/utils/math_ops.py`의 `divide` 함수 라인
   - 코멘트: *"0으로 나눌 때 ZeroDivisionError가 발생하여 서비스가 비정상 종료될 위험이 있습니다. `default: Number = 0.0` 매개변수를 추가하여 0으로 나눌 때 안전하게 기본값을 반환하도록 방어 코드를 작성하는 것은 어떨까요?"*
   - Review 상태: `Request Changes`
6. **팀원 A의 피드백 반영 및 병합:**
   - `divide(a, b, default=0.0)` 수정 커밋:
     ```bash
     git add src/utils/math_ops.py tests/test_math_ops.py
     git commit -m "feat: add zero division safeguard (apply review feedback)"
     git push origin feature/kim-math-utils
     ```
   - 팀원 B가 변경사항 확인 후 **Approve**, 팀원 C도 검토 후 **Approve** -> PR #1 `main`에 병합!

---

### 🔹 [Round 1-2] 팀원 B: 문자열 유틸리티 & 충돌 1(Hunk 충돌) 해결 (PR #2)

1. **GitHub Issue 생성:**
   - 제목: `[FEAT] 문자열 변환 및 정제 유틸리티 구현` (이슈 `#2`)
2. **브랜치 생성 및 작업:**
   ```bash
   git checkout -b feature/lee-string-utils
   # src/utils/string_ops.py 및 src/utils/__init__.py 수정
   git add src/utils/string_ops.py src/utils/__init__.py
   git commit -m "feat: add string manipulation utilities"
   ```
3. **충돌 발생 및 로컬 해결 (충돌 기록 #1):**
   - PR #1이 이미 main에 머지되어 `src/utils/__init__.py`가 변경된 상태!
   ```bash
   git fetch origin
   git merge origin/main
   # CONFLICT (content): Merge conflict in src/utils/__init__.py 발생!
   ```
   > 🔍 **GitLens / VS Code 3-Way Merge Editor 시각적 확인:**
   > - `src/utils/__init__.py` 파일을 열면 상단에 `Accept Current Change | Accept Incoming Change | Accept Both Changes` 버튼이 표시됩니다.
   > - 좌측(Current: Lee의 문자열)과 우측(Incoming: Kim의 수학)을 눈으로 비교한 뒤, 두 모듈 모두 export해야 하므로 **Accept Both Changes**를 누르거나 수동으로 정리합니다.

   - 충돌 마커 정리 후 커밋 & 푸시:
   ```bash
   git add src/utils/__init__.py
   git commit -m "fix: resolve import export conflict in __init__.py"
   git push origin feature/lee-string-utils
   ```
4. **PR #2 생성 및 리뷰/반영:**
   - 팀원 C의 리뷰: *"공백 제거 시 줄바꿈/탭 문자도 모두 고려되었는지 확인 부탁드립니다."*
   - 팀원 B의 답글: *"정규식 및 split 기반으로 탭과 줄바꿈도 완벽 제거되도록 보강 커밋 반영했습니다!"*
   - 팀원 A & C 승인 -> PR #2 `main`에 병합.

---

### 🔹 [Round 1-3] 팀원 C: 날짜/시간 포맷 유틸리티 개발 (PR #3)

1. **GitHub Issue 생성:**
   - 제목: `[FEAT] 날짜 파싱 및 ISO 포맷 변환 유틸리티 구현` (이슈 `#3`)
2. **브랜치 생성 및 작업:**
   ```bash
   git checkout -b feature/park-date-utils
   # src/utils/date_ops.py 작성
   git add src/utils/date_ops.py
   git commit -m "feat: add date parsing and formatting utilities"
   ```
3. **트러블슈팅 실습 3 (`git revert` 시뮬레이션):**
   ```bash
   # 실수로 테스트 안 된 의존성 커밋 후 원격 푸시 시뮬레이션
   git commit --allow-empty -m "feat: add experimental timezone parser"
   # 잘못된 커밋 해시 확인 후 revert 실행
   git revert HEAD --no-edit
   git push origin feature/park-date-utils
   ```
   > 🔍 **GitLens 시각적 확인 (Revert 이력 추적):**
   > - **Commit Graph**에서 이전 커밋이 삭제되는 것이 아니라, `Revert "feat: add experimental timezone parser"`라는 **신규 반대 커밋**이 그래프 맨 위에 안전하게 쌓이는 것을 확인합니다.

4. **트러블슈팅 실습 4 (`git stash` & `git stash pop`):**
   ```bash
   # 작업 중단 및 임시 보관
   git stash save "WIP: date utils relative time"
   ```
   > 🔍 **GitLens 시각적 확인 (Stash 패널):**
   > - GitLens 사이드바의 **Stashes** 섹션을 열면 저장된 작업 내역의 파일 Diff를 미리보기 할 수 있습니다.
   ```bash
   # 복원
   git stash pop
   ```
5. **PR #3 생성 및 팀원 A 리뷰 반영:**
   - 팀원 A의 피드백: *"현재 시점 기준 상대 시간(예: '3분 전', '방금 전') 변환 함수 `get_relative_time_string`도 함께 추가되면 좋겠습니다."*
   - 팀원 C가 피드백 반영 커밋 추가 후 병합 완료.

---

## 🏃 Sprint 2: 패키지 리팩토링, 비자명 충돌 & 트러블슈팅 완성

### 🔹 [Round 2-1] 팀원 A: 패키지 구조 리팩토링 (PR #4)

1. **작업 내용:**
   - `src/string_utils.py` (임시 레거시 위치) -> `src/utils/string_ops.py`로 디렉토리 구조 이동/이름 변경.
   ```bash
   git checkout -b refactor/kim-package-structure
   git commit -m "refactor: restructure utils package directory layout"
   git push origin refactor/kim-package-structure
   ```
2. **PR #4 생성 -> 팀원 B, C 리뷰 후 `main`에 병합.**

---

### 🔹 [Round 2-2] 팀원 B: 신규 함수 추가 & 비자명 충돌(Rename vs Modify) 해결 (PR #5)

1. **트러블슈팅 실습 2 (`reset --soft HEAD~1`):**
   ```bash
   git checkout -b feature/lee-advanced-strings
   # 두 가지 작업을 한 번에 잘못 커밋한 상황
   git commit -am "feat: add slugify and update docs"
   
   # soft reset으로 되돌린 후 분리 커밋
   git reset --soft HEAD~1
   ```
   > 🔍 **GitLens / VS Code Source Control 시각적 확인:**
   > - VS Code 소스 제어 패널에서 작업 내용이 삭제되지 않고 **Staged Changes(스테이지된 변경 사항)** 상태로 그대로 살아있는 것을 확인합니다.
   ```bash
   # 단일 책임 원칙에 맞게 분리 커밋
   git add src/utils/string_ops.py
   git commit -m "feat: add slugify and truncate_words utilities"
   ```

2. **비자명 충돌(Rename vs Modify) 발생 및 해결 (충돌 기록 #2):**
   - 구버전 파일 경로(`src/string_utils.py`)에 작업된 코드가 main의 리팩토링(`src/utils/string_ops.py`)과 충돌 발생.
   ```bash
   git fetch origin
   git merge origin/main
   # CONFLICT (modify/delete): src/string_utils.py deleted in origin/main and modified in HEAD.
   ```
   > 🔍 **GitLens 시각적 확인 (File History & Compare):**
   > - GitLens의 **Compare References** 또는 **File History**를 열어 `origin/main`의 `src/utils/string_ops.py`와 로컬의 `src/string_utils.py`를 좌우로 비교합니다.
   > - 로컬에 새로 추가했던 `slugify`, `truncate_words` 함수만 복사하여 새 파일 경로에 붙여넣습니다.

   ```bash
   # 변경된 새 파일로 코드 이관 후 구버전 파일 삭제
   git rm src/string_utils.py
   git add src/utils/string_ops.py
   git commit -m "refactor: migrate advanced string functions to new utils path"
   git push origin feature/lee-advanced-strings
   ```
3. **PR #5 생성 -> 승인 후 `main`에 병합.**

---

### 🔹 [Round 2-3] 팀원 C: 종합 테스트 스위트 통합 (PR #6)

1. **`tests/` 디렉토리에 전 모듈 엣지 케이스 통합 테스트 작성:**
   ```bash
   git checkout -b feature/park-test-suite
   git add tests/
   git commit -m "test: add comprehensive test suite across all modules"
   git push origin feature/park-test-suite
   ```
2. **PR #6 생성 -> 팀원 A, B 승인 후 최종 `main` 병합!**

---

## 🎯 Final Step: 제출 준비 및 최종 검증

### 1. 단위 테스트 전원 통과 확인
```bash
pytest -v tests/
```
*(14 passed in 0.3s 확인)*

### 2. 히스토리 증빙 텍스트 및 GitLens 그래프 캡처
```bash
git log --oneline --graph --all
```
> 🔍 **GitLens 시각적 증빙:**
> - GitLens의 **Commit Graph** 화면을 전체 화면으로 띄워, `feature/*` 브랜치들이 `main`으로 깔끔하게 분기 및 병합(PR Merge)된 그래프를 캡처하여 `SUBMISSION.md`에 링크 또는 첨부할 수 있습니다.

### 3. 체크리스트 최종 점검
- [x] `SUBMISSION.md` 작성 및 팀원별 PR 링크 정리
- [x] `docs/CONTRIBUTING.md` 작성 (GitHub Flow 채택 이유 3줄 포함)
- [x] `docs/conflict-resolution.md` 작성 (비자명 충돌 1건 포함 총 2건)
- [x] `docs/troubleshooting-log.md` 작성 (amend, reset soft, revert, stash 4종 및 팀원 전원 참여)
- [x] 팀원 3명 전원: PR 2개 이상 생성/병합, 리뷰 2개 이상 작성, 피드백 1회 이상 반영
- [x] Branch Protection Rule 설정으로 main 직접 push 방지 확인
