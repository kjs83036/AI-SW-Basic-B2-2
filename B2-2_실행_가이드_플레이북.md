# 🚀 B2-2 실습 가이드 플레이북 (3인 팀 완벽 실행 대본 + GitLens 시각화 가이드)

이 문서는 3명의 팀원(또는 1인 시뮬레이션)이 B2-2 미션의 모든 기능 요구사항과 평가 기준(충돌 2종, 트러블슈팅 4종, PR/리뷰)을 100% 충족하며 실습을 완료할 수 있도록 작성된 **단계별 상세 실행 매뉴얼**입니다.

> 💡 **학습 팁:** 모든 작업은 **기본 CLI 터미널 명령어**를 직접 실행하여 동작 원리를 먼저 이해하고, 각 단계마다 제공되는 **`🔍 GitLens 시각적 확인`** 팁을 통해 Git의 내부 상태(포인터, 트리, 그래프, Diff)가 어떻게 변하는지 눈으로 확인하며 학습합니다.

---

## 📋 실습 개요 및 역할 분담표

| 구분 | 팀원 A (Kim/Ko) | 팀원 B (Lee/Chae) | 팀원 C (Park) |
| :--- | :--- | :--- | :--- |
| **주요 역할** | 팀 리드 & 수학 유틸 | 문자열 유틸 & 충돌 해결 | 날짜 유틸 & 테스트 통합 |
| **Sprint 1 (기초)** | `src/math_ops.py` (PR #1) | `src/string_ops.py` (PR #2, **충돌 1: Hunk**) | `src/date_ops.py` (PR #3) |
| **Sprint 2 (심화)** | `src/utils/` 패키지 이동 (PR #4) | `slugify` 추가 (PR #5, **충돌 2: Rename vs Modify**) | 종합 테스트 스위트 (PR #6) |
| **트러블슈팅** | `commit --amend` | `reset --soft HEAD~1` | `git revert`, `git stash` |

---

## 🛠️ Step 0: 저장소 초기화 및 브랜치 보호 규칙 설정

### 1. 팀 리드(팀원 A) - GitHub 저장소 생성 및 초기 파일 커밋
```bash
# 1. 로컬 저장소 생성
mkdir team-python-utils
cd team-python-utils
git init -b main

# 2. 기본 디렉토리 생성
mkdir -p docs src/utils tests .github/ISSUE_TEMPLATE

# 3. 빈 폴더 커밋 누락 방지 및 초기 파일 준비
touch .gitignore
echo "# Team Python Utils" > README.md
touch docs/.gitkeep src/utils/.gitkeep tests/.gitkeep .github/ISSUE_TEMPLATE/.gitkeep

# 4. 첫 커밋 생성
git add .
git commit -m "chore: initial commit with basic project structure and contributing guides"

# 5. GitHub 원격 저장소 연결 및 푸시
git remote add origin https://github.com/<your-org-or-user>/team-python-utils.git
git branch -M main
git push -u origin main
```

### 2. GitHub 저장소 설정 (Branch Protection Rule)
- GitHub 저장소 웹 페이지 이동 -> **Settings** -> **Branches** -> **Add branch ruleset / protection rule**
  - **Branch name pattern:** `main`
  - ✅ **Require a pull request before merging** (체크)
  - ✅ **Require approvals:** `1` (최소 1명 승인)
  - ✅ **Do not allow bypassing the above settings**
  - Save changes

> 🔍 **GitLens 시각적 확인 (Commit Graph):**
> - VS Code 좌측 액티비티 바에서 **GitLens 아이콘** 클릭 → **Commit Graph** 패널 열기
> - `main` 브랜치와 `origin/main`이 첫 번째 커밋(`chore: initial commit...`)을 동일하게 가리키고 있는지 확인합니다.

---

## 🏃 Sprint 1: 기본 기능 구현 & 순환 코드 리뷰 & 충돌 1 실습

> **구조 안내:** Sprint 1에서는 기본 모듈들을 `src/` 루트 디렉토리에 작성하고, `src/__init__.py`를 공통 엔트리포인트로 사용하여 첫 번째 충돌(Hunk 충돌)을 유발합니다.

### 🔹 [Round 1-1] 팀원 A: 수학 유틸리티 개발 (PR #1)

1. **GitHub Issue 생성:**
   - 제목: `[FEAT] 기본 수학 연산 및 통계 유틸리티 구현` (이슈 `#1`)
2. **브랜치 생성 및 작업:**
   ```bash
   git checkout -b feature/kim-math-utils
   ```
3. **`src/math_ops.py`, `src/__init__.py`, `tests/test_math_ops.py` 작성:**
   ```python
   # src/math_ops.py
   """기본 수학 연산 함수 모듈."""

   def add(a, b):
       return a + b

   def subtract(a, b):
       return a - b

   def multiply(a, b):
       return a * b

   def divide(a, b):
       return a / b
   ```
   ```python
   # src/__init__.py
   from src.math_ops import add, divide, multiply, subtract

   __all__ = ["add", "subtract", "multiply", "divide"]
   ```
   ```python
   # tests/test_math_ops.py
   import pytest
   from src.math_ops import add, subtract, multiply, divide

   def test_basic_arithmetic():
       assert add(2, 3) == 5
       assert subtract(5, 2) == 3
       assert multiply(3, 4) == 12
       assert divide(10, 2) == 5.0
   ```

4. **트러블슈팅 실습 1 (`commit --amend`):**
   ```bash
   # 1차 커밋
   git add src/math_ops.py src/__init__.py tests/test_math_ops.py
   git commit -m "feat: add basic math utility functions"

   # 누락된 거듭제곱(power) 함수 및 docstring/타입 힌트 추가
   cat << 'EOF' >> src/math_ops.py

   def power(base: float, exponent: float) -> float:
       """거듭제곱을 계산합니다."""
       return base ** exponent
   EOF

   # 기존 커밋에 덮어쓰기 (amend)
   git add src/math_ops.py
   git commit --amend -m "feat: add power function with type annotations and docstring"
   git push origin feature/kim-math-utils
   ```
   > 🔍 **GitLens 시각적 확인 (Amend 커밋 검증):**
   > - **Commit Graph**에서 방금 작성한 커밋이 2개로 분기되지 않고, 기존 커밋 해시가 갱신되며 **단 1개의 깔끔한 커밋**으로 유지되는 것을 확인합니다.

5. **PR #1 생성 (GitHub UI):**
   - 제목: `feat: add math utility functions and exports`
   - 본문:
     ```markdown
     Closes #1
     ## 변경 사항 (What)
     - 기본 사칙연산(`add`, `subtract`, `multiply`, `divide`) 및 `power` 구현
     ## 변경 이유 (Why)
     - 공통 수치 계산 모듈 제공
     ## 테스트 (How)
     - pytest tests/test_math_ops.py 통과
     ```
6. **팀원 B의 코드 리뷰 (Line Comment):**
   - 파일: `src/math_ops.py`의 `divide` 함수 라인
   - 코멘트: *"0으로 나눌 때 ZeroDivisionError가 발생할 수 있습니다. `default: float = 0.0` 매개변수를 추가하여 방어 코드를 작성하는 것은 어떨까요?"*
   - Review 상태: `Request Changes`
7. **팀원 A의 피드백 반영 및 병합:**
   - `src/math_ops.py`의 `divide(a, b, default=0.0)` 수정 후 커밋:
     ```bash
     git add src/math_ops.py tests/test_math_ops.py
     git commit -m "feat: add zero division safeguard (apply review feedback)"
     git push origin feature/kim-math-utils
     ```
   - 팀원 B & C 검토 후 **Approve** -> PR #1 `main`에 병합!

---

### 🔹 [Round 1-2] 팀원 B: 문자열 유틸리티 & 충돌 1(Hunk 충돌) 해결 (PR #2)

1. **GitHub Issue 생성:**
   - 제목: `[FEAT] 문자열 변환 및 정제 유틸리티 구현` (이슈 `#2`)
2. **브랜치 생성 (⭐ 중요: PR #1 병합 전 상태인 `main~1`에서 분기):**
   ```bash
   git checkout -b feature/lee-string-utils main~1
   ```
3. **`src/string_ops.py`, `src/__init__.py`, `tests/test_string_ops.py` 작성:**
   ```python
   # src/string_ops.py
   """문자열 처리 유틸리티 함수 모듈."""

   def capitalize_words(text: str) -> str:
       """각 단어의 첫 글자를 대문자로 변환합니다."""
       return " ".join(word.capitalize() for word in text.split(" ")) if text else ""

   def reverse_string(text: str) -> str:
       """문자열을 반전합니다."""
       return text[::-1]

   def strip_all_whitespace(text: str) -> str:
       """모든 공백을 제거합니다."""
       return text.replace(" ", "") if text else ""
   ```
   ```python
   # src/__init__.py (팀원 A와 같은 파일의 동일 라인을 수정하여 충돌 유발)
   from src.string_ops import capitalize_words, reverse_string, strip_all_whitespace

   __all__ = ["capitalize_words", "reverse_string", "strip_all_whitespace"]
   ```
   ```python
   # tests/test_string_ops.py
   import pytest
   from src.string_ops import capitalize_words, reverse_string, strip_all_whitespace

   def test_capitalize_words():
       assert capitalize_words("hello world") == "Hello World"

   def test_reverse_string():
       assert reverse_string("hello") == "olleh"

   def test_strip_all_whitespace():
       assert strip_all_whitespace(" h e l l o ") == "hello"
   ```
4. **커밋 및 충돌 발생 (충돌 기록 #1):**
   ```bash
   git add src/string_ops.py src/__init__.py tests/test_string_ops.py
   git commit -m "feat: add string manipulation utilities"

   # PR #1이 머지된 main을 가져와 병합 시도
   git fetch origin
   git merge origin/main
   # 💥 CONFLICT (content): Merge conflict in src/__init__.py 발생!
   ```
   > 🔍 **GitLens / VS Code 3-Way Merge Editor 시각적 확인:**
   > - `src/__init__.py` 파일을 열면 상단에 `Accept Current | Accept Incoming | Accept Both` 버튼이 표시됩니다.
   > - Current(문자열 유틸)와 Incoming(수학 유틸)을 모두 export해야 하므로 수동으로 두 모듈을 모두 import하도록 정리합니다.

5. **충돌 해결 및 푸시:**
   - `src/__init__.py`를 다음과 같이 정리:
     ```python
     from src.math_ops import add, divide, multiply, power, subtract
     from src.string_ops import capitalize_words, reverse_string, strip_all_whitespace

     __all__ = [
         "add", "subtract", "multiply", "divide", "power",
         "capitalize_words", "reverse_string", "strip_all_whitespace"
     ]
     ```
   - 충돌 해결 커밋:
     ```bash
     git add src/__init__.py
     git commit -m "fix: resolve import export conflict in __init__.py"
     git push origin feature/lee-string-utils
     ```
6. **PR #2 생성 및 리뷰/반영:**
   - 팀원 C의 리뷰: *"공백 제거 시 탭/줄바꿈 문자도 제거되도록 보강해주세요."*
   - 팀원 B가 `"".join(text.split())`으로 보강 커밋 후 푸시 -> 승인 및 `main` 병합!

---

### 🔹 [Round 1-3] 팀원 C: 날짜/시간 유틸리티 & 트러블슈팅 (PR #3)

1. **GitHub Issue 생성:**
   - 제목: `[FEAT] 날짜 파싱 및 ISO 포맷 변환 유틸리티 구현` (이슈 `#3`)
2. **최신 main에서 브랜치 생성 및 작업:**
   ```bash
   git checkout main
   git pull origin main
   git checkout -b feature/park-date-utils
   ```
3. **`src/date_ops.py`, `tests/test_date_ops.py` 작성 및 `src/__init__.py` export 추가:**
   ```python
   # src/date_ops.py
   """날짜 및 시간 처리 유틸리티 함수 모듈."""
   from datetime import datetime, timedelta
   from typing import Optional

   def format_iso_date(dt: Optional[datetime] = None) -> str:
       """날짜를 ISO 포맷(YYYY-MM-DD)으로 변환합니다."""
       if dt is None:
           dt = datetime.now()
       return dt.strftime("%Y-%m-%d")

   def add_days_to_date(dt: datetime, days: int) -> datetime:
       """날짜에 일수를 더합니다."""
       return dt + timedelta(days=days)
   ```
4. **트러블슈팅 실습 3 (`git revert` 시뮬레이션):**
   ```bash
   # 실수로 테스트되지 않은 모듈을 커밋한 상황 시뮬레이션
   git commit --allow-empty -m "feat: add experimental timezone parser"

   # 잘못된 직전 커밋 취소하는 반대 커밋 생성
   git revert HEAD --no-edit
   ```
   > 🔍 **GitLens 시각적 확인 (Revert 이력 추적):**
   > - **Commit Graph**에서 이전 커밋이 삭제되지 않고 `Revert "feat: add experimental timezone parser"`라는 **신규 반대 커밋**이 안전하게 쌓인 것을 확인합니다.

5. **트러블슈팅 실습 4 (`git stash` & `git stash pop`):**
   ```bash
   # 작업 중단 및 임시 보관
   echo "# WIP relative time calculation" >> src/date_ops.py
   git stash save "WIP: date utils relative time"

   # 메인 브랜치 점검 후 복원
   git stash pop
   ```
6. **피드백 반영 (`get_relative_time_string` 추가) 후 커밋 & 푸시:**
   ```bash
   git add src/date_ops.py src/__init__.py tests/test_date_ops.py
   git commit -m "feat: add date parsing, formatting, and relative time utilities"
   git push origin feature/park-date-utils
   ```
7. **PR #3 생성 -> 승인 후 `main` 병합.**

---

## 🏃 Sprint 2: 패키지 리팩토링, 비자명 충돌 & 트러블슈팅 완성

---

### 🔹 [Round 2-1] 팀원 A: 패키지 구조 리팩토링 (PR #4)

1. **작업 목적:** `src/`에 위치하던 모듈들을 정규 패키지 구조인 `src/utils/` 디렉토리 하위로 일괄 이동 정리.
2. **브랜치 생성 및 파일 이동:**
   ```bash
   git checkout main
   git pull origin main
   git checkout -b refactor/kim-package-structure

   # src/ 하위 모듈들을 src/utils/ 로 이동 (Rename/Move)
   git mv src/math_ops.py src/utils/math_ops.py
   git mv src/string_ops.py src/utils/string_ops.py
   git mv src/date_ops.py src/utils/date_ops.py
   git mv src/__init__.py src/utils/__init__.py

   # 협업 문서 docs/CONTRIBUTING.md 작성
   git add docs/CONTRIBUTING.md
   git commit -m "refactor: restructure package modules into src/utils/ directory layout"
   git push origin refactor/kim-package-structure
   ```
3. **PR #4 생성 -> 팀원 B & C 리뷰 후 `main`에 병합.**

---

### 🔹 [Round 2-2] 팀원 B: 신규 함수 추가 & 비자명 충돌(Rename vs Modify) 해결 (PR #5)

1. **브랜치 생성 (⭐ 중요: PR #4 리팩토링 머지 직전 시점인 `main~1`에서 분기):**
   ```bash
   git checkout -b feature/lee-advanced-strings main~1
   ```
2. **구버전 파일(`src/string_ops.py`)에 신규 함수(`slugify`, `truncate_words`) 추가 및 임시 파일 작성:**
   ```python
   # src/string_ops.py 끝에 추가
   import re

   def slugify(text: str) -> str:
       """URL 친화적인 slug 문자열로 변환합니다."""
       if not text:
           return ""
       text = re.sub(r"[^\w\s-]", "", text.lower().strip())
       return re.sub(r"[\s_-]+", "-", text).strip("-")

   def truncate_words(text: str, max_words: int, suffix: str = "...") -> str:
       """지정된 단어 수를 초과하는 문자열을 축약합니다."""
       words = text.split()
       return " ".join(words[:max_words]) + suffix if len(words) > max_words else text
   ```
3. **트러블슈팅 실습 2 (`reset --soft HEAD~1`):**
   ```bash
   echo "# Temp Notes" > docs/temp_notes.md
   git add src/string_ops.py docs/temp_notes.md
   git commit -m "feat: add slugify and update docs"

   # soft reset으로 되돌린 후 문서 파일 분리 및 삭제
   git reset --soft HEAD~1
   git reset HEAD docs/temp_notes.md
   rm docs/temp_notes.md

   # 단일 책임 커밋
   git add src/string_ops.py
   git commit -m "feat: add slugify and truncate_words utilities"
   ```

4. **비자명 충돌(Rename vs Modify) 발생 (충돌 기록 #2):**
   ```bash
   git fetch origin
   git merge origin/main
   # 💥 CONFLICT (modify/delete): src/string_ops.py deleted in origin/main and modified in HEAD.
   ```
   > 🔍 **GitLens 시각적 확인 (Compare & File History):**
   > - `main`에서는 `src/string_ops.py`가 `src/utils/string_ops.py`로 이동(삭제)되었고, 로컬 `HEAD`에서는 `src/string_ops.py`를 수정한 상태입니다.

5. **비자명 충돌 해결:**
   - 로컬 구버전(`src/string_ops.py`)에 새로 작성한 `slugify`, `truncate_words` 함수를 신규 경로 파일(`src/utils/string_ops.py`)로 이관합니다.
   - 구버전 파일 삭제 및 새 파일 커밋:
     ```bash
     git rm src/string_ops.py
     git add src/utils/string_ops.py
     git commit -m "refactor: migrate advanced string functions to new utils path"
     git push origin feature/lee-advanced-strings
     ```
6. **PR #5 생성 -> 승인 후 `main`에 병합.**

---

### 🔹 [Round 2-3] 팀원 C: 종합 테스트 스위트 통합 (PR #6)

1. **브랜치 생성 및 작업:**
   ```bash
   git checkout main
   git pull origin main
   git checkout -b feature/park-test-suite
   ```
2. **`tests/` 내 테스트 파일들의 import 경로를 `src.utils.*`로 통합 갱신 및 엣지 케이스 테스트 추가.**
3. **`src/utils/__init__.py`의 `__all__` 목록 최종 갱신.**
4. **커밋 및 푸시:**
   ```bash
   git add tests/ src/utils/__init__.py
   git commit -m "test: add comprehensive test suite across all modules"
   git push origin feature/park-test-suite
   ```
5. **PR #6 생성 -> 승인 후 최종 `main` 병합!**

---

## 🎯 Final Step: 제출 준비 및 최종 검증

### 1. 단위 테스트 전원 통과 확인
```bash
python -m pytest -v tests/
```
*(전체 테스트 통과 확인)*

### 2. 히스토리 증빙 텍스트 및 GitLens 그래프 캡처
```bash
git log --oneline --graph --all
```
> 🔍 **GitLens 시각적 증빙:**
> - GitLens의 **Commit Graph** 화면에서 모든 `feature/*` 브랜치와 PR 병합 이력이 깔끔하게 연결된 그래프를 캡처하여 `SUBMISSION.md`에 첨부합니다.

### 3. 체크리스트 최종 점검
- [x] `SUBMISSION.md` 작성 및 팀원별 PR 링크 정리
- [x] `docs/CONTRIBUTING.md` 작성 (GitHub Flow 채택 이유 3줄 포함)
- [x] `docs/conflict-resolution.md` 작성 (Hunk 충돌 1건 + Rename vs Modify 비자명 충돌 1건)
- [x] `docs/troubleshooting-log.md` 작성 (amend, reset soft, revert, stash 4종 및 팀원 전원 참여)
- [x] 팀원 3명 전원: PR 2개 이상 생성/병합, 리뷰 2개 이상 작성, 피드백 1회 이상 반영
- [x] Branch Protection Rule 설정으로 main 직접 push 방지 확인

